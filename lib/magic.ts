// Magic embedded wallet: recipient onboarding via Google/email login.
// Acts as the recipient's initial signer before the EOA is upgraded to a
// Universal Account via EIP-7702 on first claim.
//
// All SDK access lives in /lib. UI components stay presentational.
// TODO(week3): wire magic-sdk once dependency is confirmed.

export {};
