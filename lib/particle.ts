// Particle Network Universal Accounts SDK (EIP-7702 mode, V2).
// Handles cross-chain routing for funding (sender) and cash-out (recipient),
// and the on-the-fly EOA -> UA upgrade at first claim.
//
// Cross-chain routing is the SDK's job, not the GiftEscrow contract.
// TODO(week1): wire @particle-network/universal-account once confirmed.

export {};
