# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (secrets: Particle, Magic, ZeroDev, RPC, Supabase)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# typescript
*.tsbuildinfo

# vercel
.vercel

# editor / ide
.idea/
.vscode/
*.swp

# claude local settings (machine-specific)
.claude/settings.local.json

# contracts build artifacts + vendored libs (restore via: forge install)
/contracts/out/
/contracts/cache/
/contracts/broadcast/
/contracts/lib/
/artifacts/

# logs
*.log

# local npm config (registry mirror, machine-specific)
.npmrc

# internal AI agent instructions (kept local, not for public repo)
CLAUDE.md
