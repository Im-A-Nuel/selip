feat: remove Apple login; QR + custom image + recipient name on dashboard; OG custom image; claim email

- Remove the Apple sign-in option (provider not configured; would error). Google
  and email OTP remain.
- Dashboard: per-gift QR button (opens the QR modal), shows "To <name>", and a
  custom card-image thumbnail instead of the occasion icon.
- OG image: custom gifts render their drawn/uploaded image as the share preview
  background.
- Recipient name surfaced on the dashboard (status API now returns it).
- Email notification to the sender when a gift is opened: optional "Notify me"
  email at create time -> Resend on claim (lib/email.ts). Best-effort, no-op
  without RESEND_API_KEY. New column via migration 0006; Supabase insert only
  sends sender_email when present (migration-safe).

Config: run 0006 on Supabase; set RESEND_API_KEY + RESEND_FROM for real emails.
